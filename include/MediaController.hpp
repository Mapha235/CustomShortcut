#pragma once
namespace multimedia{
    // enum class Media{PLAY_PAUSE};
    bool PlayPause();
    bool NextTrack();
    bool PrevTrack();
    bool VolumeDown();
    bool VolumeUp();
    bool Mute();
}